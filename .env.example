# Example env file (do NOT use in production)
# Copy to .env.local and fill your real keys locally only

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=ey...
# Only for server-side code (never expose to browser)
# SUPABASE_SERVICE_ROLE=ey...
